# =============================================
# THRIFT-KE ENVIRONMENT VARIABLES
# =============================================
# Copy this file to .env.local and fill in your values
# cp .env.example .env.local

# =============================================
# SUPABASE (Required)
# =============================================
# Get these from: Supabase Dashboard > Project Settings > API

# Your Supabase project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase anon/public key (safe to expose in browser)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase service role key (KEEP SECRET - server-side only)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# =============================================
# APP CONFIGURATION
# =============================================

# Your app URL (use localhost for development). Used for OAuth redirects (Google sign-in).
# Must match the URL you add in Supabase Dashboard > Auth > URL Configuration > Redirect URLs.
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Secret key for JWT session tokens (generate a random string)
# You can generate one with: openssl rand -base64 32
JWT_SECRET=your-random-secret-key-here

# =============================================
# AFRICA'S TALKING SMS (Optional)
# =============================================
# Get these from: africastalking.com dashboard
# Leave empty to use console OTP logging in development

AT_API_KEY=
AT_USERNAME=sandbox
AT_SENDER_ID=THRIFTKE
